# ── Application ──────────────────────────────────────────────
spring.application.name=gym-management
server.port=8080

# ── Database H2 FILE-BASED (data lưu lại sau khi restart) ────
spring.datasource.url=jdbc:h2:file:./gymdb;DB_CLOSE_DELAY=-1;AUTO_SERVER=TRUE;NON_KEYWORDS=VALUE
spring.datasource.driver-class-name=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=
spring.jpa.database-platform=org.hibernate.dialect.H2Dialect
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ── JPA — dùng update thay vì create-drop ────────────────────
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false

# ── JWT ───────────────────────────────────────────────────────
jwt.secret=GymAppSuperSecretKeyForJWT2024AtLeast256BitsLong!
jwt.expiration=86400000

# ── App ───────────────────────────────────────────────────────
app.base-url=http://localhost:8080

# Email OTP (cấu hình bằng biến môi trường khi triển khai thật)
spring.mail.host=${MAIL_HOST:}
spring.mail.port=${MAIL_PORT:587}
spring.mail.username=${MAIL_USERNAME:}
spring.mail.password=${MAIL_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true
shop.otp.enabled=${SHOP_OTP_ENABLED:false}

# ── File upload (đính kèm chat) ───────────────────────────────
app.upload-dir=./uploads
spring.servlet.multipart.max-file-size=50MB
spring.servlet.multipart.max-request-size=50MB

# ── Scheduling ────────────────────────────────────────────────
spring.task.scheduling.enabled=true

# ── Logging ───────────────────────────────────────────────────
logging.level.com.example.gymmanagement=INFO
logging.level.org.springframework.security=WARN
# ── Chuyển khoản ngân hàng (VietQR + SePay webhook) ──────────────
# Mã BIN ngân hàng theo chuẩn VietQR, tra tại: https://api.vietqr.io/v2/banks
bank.id=970422
bank.account-no=0961702083
bank.account-name=LE ANH DUC

# dashboard SePay (mục Webhook -> Authorization Header)
sepay.webhook-api-key=doi-thanh-chuoi-bi-mat-cua-ban
